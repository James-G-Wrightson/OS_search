name: os_search
channels:
  - conda-forge
dependencies:
  # System / language toolchain only. R package versions are pinned in
  # renv.lock and installed by `renv::restore()` after env activation.
  - r-base=4.5
  - r-renv
  - python=3.12
  - pip
  - numpy
